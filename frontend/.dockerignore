node_modules/
dist/
.vite/

# The map file and the region definition belong to the place, not to the software: large, rarely
# changed, and different per museum. They are mounted at runtime (docker-compose.yml).
public/tiles/
